# Deploy Render - Checklist

- [ ] Ajustar script `build` do `server/package.json` para suportar o comando do Render.
- [x] Reexecutar o build local (raiz e server) para validar.
- [x] Garantir que o Render “Build command” funcione sem erro de script inexistente.


