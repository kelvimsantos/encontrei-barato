# Deploy Render - Checklist

- [ ] Ajustar script `build` do `server/package.json` para suportar o comando do Render.
- [ ] Reexecutar o build local (raiz e server) para validar.
- [ ] Garantir que o Render “Build command” funcione sem erro de script inexistente.

